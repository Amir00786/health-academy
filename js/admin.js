window.I18N_PAGE_DICT = {
  'admin.title': { en: 'Owner Console', ar: 'وحدة تحكم المالك' },
  'admin.lockSub': { en: 'Enter the owner passcode to continue.', ar: 'أدخل رمز مرور المالك للمتابعة.' },
  'admin.passcodePh': { en: 'Passcode', ar: 'رمز المرور' },
  'admin.unlock': { en: 'Unlock', ar: 'فتح' },
  'admin.lockNote': { en: 'This is a basic client-side passcode, not real authentication — anyone who inspects the page source can bypass it. Add a real server-side login before relying on this to keep the console private.', ar: 'هذا مجرد رمز مرور أساسي من جهة العميل، وليس مصادقة حقيقية — يمكن لأي شخص يفحص كود الصفحة تجاوزه. أضف نظام تسجيل دخول حقيقي من جهة الخادم قبل الاعتماد على هذا للحفاظ على خصوصية الوحدة.' },
  'admin.roleCeo': { en: 'CEO', ar: 'الرئيس التنفيذي' },
  'admin.exitLink': { en: '<i class="fi fi-rr-arrow-small-left"></i> Exit to public site', ar: '<i class="fi fi-rr-arrow-small-right"></i> الخروج إلى الموقع العام' },
  'admin.demoBannerStrong': { en: 'Demo data.', ar: 'بيانات تجريبية.' },
  'admin.demoBannerText': { en: "This console isn't connected to a real analytics or payments backend yet, so visitor counts, sign-ups, revenue, and traffic sources below show as empty until that's wired up. The Trust & Safety and Mentor review sections below use sample entries to demonstrate the workflow — but the Review / Approve / Reject / Suspend buttons are fully functional and their state is saved in this browser.", ar: 'هذه الوحدة غير متصلة بعد بأي نظام تحليلات أو مدفوعات حقيقي، لذا تظهر أعداد الزوار والتسجيلات والإيرادات ومصادر الزيارات أدناه فارغة حتى يتم ربطها. تستخدم أقسام السلامة والثقة ومراجعة الموجهين أدناه بيانات نموذجية لتوضيح آلية العمل — لكن أزرار مراجعة / موافقة / رفض / تعليق تعمل بشكل كامل وتُحفظ حالتها في هذا المتصفح.' },
  'admin.briefingTitle': { en: "🧭 This week's briefing", ar: '🧭 ملخص هذا الأسبوع' },
  'admin.briefingEmpty': { en: 'No briefing yet — this section will summarize real activity once analytics and the moderation/mentor pipelines are connected to a backend.', ar: 'لا يوجد ملخص بعد — سيقوم هذا القسم بتلخيص النشاط الحقيقي بمجرد ربط التحليلات وأنظمة الإشراف والموجهين بخادم حقيقي.' },
  'admin.kpiVisitors': { en: 'Visitors (30d)', ar: 'الزوار (30 يومًا)' },
  'admin.kpiSignups': { en: 'Sign-ups (30d)', ar: 'التسجيلات (30 يومًا)' },
  'admin.kpiPaidMembers': { en: 'Paid members', ar: 'الأعضاء المدفوعون' },
  'admin.kpiMRR': { en: 'MRR', ar: 'الإيراد الشهري المتكرر' },
  'admin.funnelTitle': { en: 'Visitor <i class="fi fi-rr-arrow-small-right"></i> paid funnel (30d)', ar: 'مسار الزائر <i class="fi fi-rr-arrow-small-left"></i> المشترك المدفوع (30 يومًا)' },
  'admin.funnelVisitors': { en: 'Visitors', ar: 'الزوار' },
  'admin.funnelSignedUp': { en: 'Signed up', ar: 'مسجّلون' },
  'admin.funnelActivated': { en: 'Activated', ar: 'مُفعَّلون' },
  'admin.funnelActivatedSub': { en: '(1+ module done)', ar: '(أكملوا وحدة واحدة على الأقل)' },
  'admin.funnelPaid': { en: 'Paid', ar: 'مدفوعون' },
  'admin.notConnected': { en: 'Not yet connected to live data.', ar: 'غير متصل بعد ببيانات حقيقية.' },
  'admin.trackEngagement': { en: 'Track engagement', ar: 'تفاعل المسارات' },
  'admin.preAuthLabel': { en: 'Pre-Auth Specialist', ar: 'أخصائي التصريح المسبق' },
  'admin.radiologyLabel': { en: 'Radiology', ar: 'الأشعة' },
  'admin.revenueByPlan': { en: 'Revenue by plan', ar: 'الإيرادات حسب الخطة' },
  'admin.planFree': { en: 'Free', ar: 'مجاني' },
  'admin.planPro': { en: 'Pro', ar: 'برو' },
  'admin.planInstitution': { en: 'Institution', ar: 'المؤسسات' },
  'admin.importCsv': { en: 'Import PayPal CSV', ar: 'استيراد ملف PayPal CSV' },
  'admin.recentTransactions': { en: '💳 Recent transactions', ar: '💳 المعاملات الأخيرة' },
  'admin.transactionsNote': { en: 'Imported from a PayPal CSV export — stored only in this browser (included automatically in the Export backup below).', ar: 'مستوردة من ملف تصدير PayPal CSV — مخزّنة فقط في هذا المتصفح (تُضمَّن تلقائيًا في النسخة الاحتياطية للتصدير أدناه).' },
  'admin.trustSafety': { en: '⚠️ Trust & Safety queue', ar: '⚠️ قائمة السلامة والثقة' },
  'admin.trustSafetyNote': { en: 'Sample entries — connect a real reporting/moderation backend to see live reports.', ar: 'إدخالات نموذجية — اربط نظام إبلاغ/إشراف حقيقي لرؤية البلاغات الفعلية.' },
  'admin.mentorOps': { en: '👥 Mentor operations', ar: '👥 عمليات الموجهين' },
  'admin.activeMentors': { en: 'Active mentors', ar: 'الموجهون النشطون' },
  'admin.pendingReview': { en: 'Pending review', ar: 'قيد المراجعة' },
  'admin.avgRating': { en: 'Avg. rating', ar: 'متوسط التقييم' },
  'admin.mentorOpsNote': { en: 'Sample applicants — connect a real mentor application form to see live submissions.', ar: 'متقدمون نموذجيون — اربط نموذج تقديم موجهين حقيقي لرؤية الطلبات الفعلية.' },
  'admin.meetingsOverview': { en: '📅 Meetings overview', ar: '📅 نظرة عامة على الاجتماعات' },
  'admin.pendingRequests': { en: 'Pending requests', ar: 'طلبات معلّقة' },
  'admin.accepted': { en: 'Accepted', ar: 'مقبولة' },
  'admin.declined': { en: 'Declined', ar: 'مرفوضة' },
  'admin.meetingsNote': { en: 'Reflects mentor/student meeting requests stored in this browser — connect a real backend for a cross-device view.', ar: 'يعكس طلبات اجتماعات الموجهين/الطلاب المخزّنة في هذا المتصفح — اربط خادمًا حقيقيًا لعرضها عبر جميع الأجهزة.' },
  'admin.courseFeedback': { en: '⭐ Course feedback', ar: '⭐ ملاحظات الدورة' },
  'admin.courseFeedbackNote': { en: 'Star ratings and written comments students leave after finishing a course — stored only in this browser (included automatically in the Export backup below). Remove anything irrelevant or inappropriate; removal is immediate.', ar: 'تقييمات بالنجوم وتعليقات مكتوبة يتركها الطلاب بعد إنهاء الدورة — مخزّنة فقط في هذا المتصفح (تُضمَّن تلقائيًا في النسخة الاحتياطية للتصدير أدناه). احذف أي تعليق غير ذي صلة أو غير لائق؛ يتم الحذف فورًا.' },
  'admin.backupRestore': { en: '🗂️ Backup & Restore', ar: '🗂️ النسخ الاحتياطي والاستعادة' },
  'admin.backupNote': { en: 'Export everything stored in this browser — student progress, mentor directory, meeting requests, and admin decisions — as a JSON file you can keep safe, and re-import it later to restore it or move it to another browser.', ar: 'صدّر كل ما هو مخزّن في هذا المتصفح — تقدم الطلاب، دليل الموجهين، طلبات الاجتماعات، وقرارات الإدارة — كملف JSON يمكنك حفظه بأمان، وإعادة استيراده لاحقًا لاستعادته أو نقله إلى متصفح آخر.' },
  'admin.exportBtn': { en: 'Export data (.json)', ar: 'تصدير البيانات (.json)' },
  'admin.importBtn': { en: 'Import data (.json)', ar: 'استيراد البيانات (.json)' },
  'admin.waitlistDemand': { en: 'Waitlist demand', ar: 'الطلب على قائمة الانتظار' },
  'admin.waitlistMentorships': { en: 'Mentorships', ar: 'برامج الإرشاد' },
  'admin.waitlistManagers': { en: 'Managers Professional', ar: 'المدراء المحترفون' },
  'admin.waitlistSupporters': { en: 'Supporters', ar: 'الداعمون' },
  'admin.waitlistCofounders': { en: 'Co-Founders', ar: 'الشركاء المؤسسون' },
  'admin.waitlistNote': { en: 'There\'s no waitlist-capture form behind these "Coming soon" nav items yet — add one to start collecting real demand data here.', ar: 'لا يوجد نموذج لتسجيل قائمة الانتظار خلف عناصر التنقل "قريبًا" هذه بعد — أضف واحدًا لبدء جمع بيانات طلب حقيقية هنا.' },
  'admin.signupSources': { en: 'Where sign-ups come from', ar: 'مصادر التسجيلات' },
  'admin.srcInstagram': { en: 'Instagram', ar: 'إنستغرام' },
  'admin.srcWhatsapp': { en: 'WhatsApp', ar: 'واتساب' },
  'admin.srcDirect': { en: 'Direct', ar: 'مباشر' },
  'admin.srcTwitter': { en: 'X / Twitter', ar: 'إكس / تويتر' },
  'admin.srcLinkedin': { en: 'LinkedIn', ar: 'لينكدإن' },
  'admin.srcNote': { en: 'Not yet connected to a real analytics source.', ar: 'غير متصل بعد بمصدر تحليلات حقيقي.' },
};

// OWNER CONSOLE — client-side passcode gate (NOT real security, see note in admin.html)
// plus locally-persisted Trust & Safety / Mentor review actions.
(function () {
  const PASSCODE = 'ihealth-admin-2026';
  const AUTH_KEY = 'ih-admin-authed';
  const RESOLVED_REPORTS_KEY = 'ih-admin-resolved-reports';
  const MENTOR_DECISIONS_KEY = 'ih-admin-mentor-decisions';
  const COURSE_FEEDBACK_KEY = 'ih-course-feedback';

  // Maps a course id (as tagged on each feedback entry) to an existing dictionary
  // label, so new course pages just need to add their id here to show up correctly.
  const COURSE_FEEDBACK_LABELS = {
    'insurance-dept': 'admin.preAuthLabel',
    'radiology-dept': 'admin.radiologyLabel',
  };

  const SAMPLE_REPORTS = [
    { id: 1042, title: 'Possible exam-answer sharing in a mentor session', meta: 'Report #1042 · Student account', severity: 'high', actions: [{ label: 'Review', cls: '' }, { label: 'Suspend', cls: 'danger' }] },
    { id: 1035, title: 'Inappropriate message reported by a student', meta: 'Report #1035 · Mentor account', severity: 'high', actions: [{ label: 'Review', cls: '' }, { label: 'Suspend', cls: 'danger' }] },
    { id: 1041, title: 'Mentor profile using a stock photo, unverified credentials', meta: 'Report #1041 · Mentor application', severity: 'medium', actions: [{ label: 'Review', cls: '' }, { label: 'Reject', cls: 'danger' }] },
    { id: 1038, title: '12 sign-ups from the same IP range in one hour', meta: 'Report #1038 · Possible spam', severity: 'medium', actions: [{ label: 'Review', cls: '' }, { label: 'Block', cls: 'danger' }] },
  ];

  const SAMPLE_MENTORS = [
    { id: 214, name: 'Applicant #214', meta: 'Radiology · 8 yrs experience' },
    { id: 215, name: 'Applicant #215', meta: 'Pre-Auth Specialist · 5 yrs experience' },
    { id: 216, name: 'Applicant #216', meta: 'Radiology · 11 yrs experience' },
  ];

  // Local i18n for dynamic queue/table UI built via innerHTML in this file.
  // The shared dictionary (js/i18n.js) only re-tags static [data-i18n] elements,
  // so anything rendered here needs its own lang lookup at render time.
  function currentLang() {
    return (window.I18N && window.I18N.currentLang()) || 'en';
  }

  function t(key) {
    const entry = STRINGS[key];
    if (!entry) return key;
    return entry[currentLang()] || entry.en;
  }

  const STRINGS = {
    incorrectPasscode: { en: 'Incorrect passcode.', ar: 'رمز مرور غير صحيح.' },
    modQueueClear: { en: 'Queue is clear — nothing waiting on review.', ar: 'القائمة خالية — لا يوجد ما يحتاج إلى مراجعة.' },
    caseReviewAlert: { en: 'Opening a real case-review view needs a backend to store report details/evidence — this button is a placeholder for that flow.', ar: 'فتح صفحة مراجعة حالة حقيقية يتطلب خادمًا لتخزين تفاصيل البلاغ والأدلة — هذا الزر مجرد نموذج مؤقت لهذه العملية.' },
    noPendingMentors: { en: 'No pending mentor applications.', ar: 'لا توجد طلبات موجهين قيد المراجعة.' },
    noMeetings: { en: 'No meeting requests yet.', ar: 'لا توجد طلبات اجتماعات حتى الآن.' },
    noDateSet: { en: 'No date set', ar: 'لم يُحدَّد تاريخ' },
    backupRestored: { en: 'Backup restored — reloading…', ar: 'تمت استعادة النسخة الاحتياطية — جارٍ إعادة التحميل…' },
    backupBadFile: { en: "Could not read that file — make sure it's a backup JSON exported from this console.", ar: 'تعذّرت قراءة هذا الملف — تأكد من أنه ملف نسخة احتياطية بصيغة JSON صادر من هذه الوحدة.' },
    noTransactions: { en: 'No transactions imported yet.', ar: 'لم تُستورد أي معاملات حتى الآن.' },
    unknownPayer: { en: 'Unknown', ar: 'غير معروف' },
    revenueNotConnected: { en: "Not yet connected — import a PayPal Activity CSV export below. This panel reads from a swappable data layer (js/revenue.js), so plugging in a live PayPal/Stripe API later won't require redesigning it.", ar: 'غير متصل بعد — استورد ملف تصدير نشاط PayPal (CSV) أدناه. تعتمد هذه اللوحة على طبقة بيانات قابلة للتبديل (js/revenue.js)، لذا فإن ربط واجهة PayPal/Stripe حقيقية لاحقًا لن يتطلب إعادة تصميمها.' },
    revenueConnectedNote: { en: 'From an imported PayPal CSV ({count} transaction(s)). Import a newer export to refresh — connecting a live PayPal/Stripe API later reuses this same panel.', ar: 'من ملف PayPal CSV مستورد ({count} معاملة). استورد نسخة أحدث لتحديث البيانات — سيُعاد استخدام هذه اللوحة نفسها عند ربط واجهة PayPal/Stripe حقيقية لاحقًا.' },
    transactionsImportedAlert: { en: '{count} transaction(s) imported.', ar: 'تم استيراد {count} معاملة.' },
    noFeedback: { en: 'No course feedback submitted yet.', ar: 'لم تُقدَّم أي ملاحظات على الدورات حتى الآن.' },
    removeFeedback: { en: 'Remove', ar: 'حذف' },
    confirmRemoveFeedback: { en: 'Remove this feedback entry? This cannot be undone.', ar: 'هل تريد حذف هذا التعليق؟ لا يمكن التراجع عن هذا الإجراء.' },
    anonymousStudent: { en: 'Anonymous student', ar: 'طالب مجهول' },
  };

  // Small fixed sets of repeating labels — status/severity words and action
  // button text — kept separate from STRINGS since they're keyed by a data value.
  const SEVERITY_LABELS = {
    high: { en: 'HIGH', ar: 'خطورة عالية' },
    medium: { en: 'MEDIUM', ar: 'متوسطة' },
    low: { en: 'LOW', ar: 'منخفضة' },
  };

  const ACTION_LABELS = {
    Review: { en: 'Review', ar: 'مراجعة' },
    Suspend: { en: 'Suspend', ar: 'تعليق' },
    Reject: { en: 'Reject', ar: 'رفض' },
    Block: { en: 'Block', ar: 'حظر' },
    Approve: { en: 'Approve', ar: 'قبول' },
  };

  const MEETING_STATUS_LABELS = {
    pending: { en: 'Pending', ar: 'قيد الانتظار' },
    accepted: { en: 'Accepted', ar: 'مقبولة' },
    declined: { en: 'Declined', ar: 'مرفوضة' },
  };

  const PLAN_LABELS = {
    Free: { en: 'Free', ar: 'مجاني' },
    Pro: { en: 'Pro', ar: 'برو' },
    Institution: { en: 'Institution', ar: 'المؤسسات' },
  };

  function label(map, key) {
    const entry = map[key];
    if (!entry) return key;
    return entry[currentLang()] || entry.en;
  }

  // Reads a key straight out of the shared/page i18n dictionary (js/i18n.js) —
  // used when a hardcoded string here just duplicates an existing [data-i18n] key.
  function dictLabel(key) {
    if (!window.I18N) return key;
    const entry = window.I18N.dict()[key];
    if (!entry) return key;
    return entry[currentLang()] || entry.en;
  }

  function loadJson(key) {
    try {
      return JSON.parse(localStorage.getItem(key)) || [];
    } catch (e) {
      return [];
    }
  }

  function initLock() {
    const lock = document.getElementById('adminLock');
    const console_ = document.getElementById('adminConsole');
    const input = document.getElementById('adminPasscode');
    const btn = document.getElementById('adminUnlockBtn');
    const error = document.getElementById('adminLockError');

    function unlock() {
      lock.hidden = true;
      console_.hidden = false;
    }

    if (localStorage.getItem(AUTH_KEY) === '1') {
      unlock();
      return;
    }

    function tryUnlock() {
      if (input.value === PASSCODE) {
        localStorage.setItem(AUTH_KEY, '1');
        unlock();
      } else {
        error.textContent = t('incorrectPasscode');
        input.value = '';
        input.focus();
      }
    }

    btn.addEventListener('click', tryUnlock);
    input.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') tryUnlock();
    });
    input.focus();
  }

  function renderModQueue() {
    const list = document.getElementById('modQueueList');
    const resolved = loadJson(RESOLVED_REPORTS_KEY);
    const remaining = SAMPLE_REPORTS.filter((r) => !resolved.includes(r.id));
    list.innerHTML = '';
    if (!remaining.length) {
      list.innerHTML = '<p class="empty-note">' + t('modQueueClear') + '</p>';
      return;
    }
    remaining.forEach((r) => {
      const row = document.createElement('div');
      row.className = 'mod-row';
      row.innerHTML =
        '<div><div class="mod-title">' + r.title + '</div><div class="mod-meta">' + r.meta + '</div></div>' +
        '<div style="text-align:right;">' +
          '<span class="mod-severity ' + r.severity + '">' + label(SEVERITY_LABELS, r.severity) + '</span>' +
          '<div class="mod-actions" style="margin-top:8px;">' +
            r.actions.map((a) => '<button type="button" class="mod-btn ' + a.cls + '" data-report="' + r.id + '" data-action="' + a.label + '">' + label(ACTION_LABELS, a.label) + '</button>').join('') +
          '</div>' +
        '</div>';
      list.appendChild(row);
    });

    list.querySelectorAll('.mod-btn').forEach((b) => {
      b.addEventListener('click', () => {
        const action = b.getAttribute('data-action');
        if (action === 'Review') {
          alert(t('caseReviewAlert'));
          return;
        }
        const id = Number(b.getAttribute('data-report'));
        const resolved = loadJson(RESOLVED_REPORTS_KEY);
        resolved.push(id);
        localStorage.setItem(RESOLVED_REPORTS_KEY, JSON.stringify(resolved));
        renderModQueue();
      });
    });
  }

  function renderMentorQueue() {
    const list = document.getElementById('mentorQueueList');
    const decisions = loadJson(MENTOR_DECISIONS_KEY);
    const decidedIds = decisions.map((d) => d.id);
    const remaining = SAMPLE_MENTORS.filter((m) => !decidedIds.includes(m.id));
    // data-i18n keeps this span in sync if js/i18n.js's own apply() runs again;
    // the langchange re-render below also refreshes it directly.
    document.getElementById('mentorPendingMetric').innerHTML =
      '<b style="font-size:20px;">' + remaining.length + '</b><span data-i18n="admin.pendingReview">' + dictLabel('admin.pendingReview') + '</span>';
    list.innerHTML = '';
    if (!remaining.length) {
      list.innerHTML = '<p class="empty-note">' + t('noPendingMentors') + '</p>';
      return;
    }
    remaining.forEach((m) => {
      const row = document.createElement('div');
      row.className = 'mentor-row';
      row.innerHTML =
        '<div><div class="mentor-name">' + m.name + '</div><div class="mentor-meta">' + m.meta + '</div></div>' +
        '<div class="mod-actions">' +
          '<button type="button" class="mod-btn" data-mentor="' + m.id + '" data-decision="approved">' + label(ACTION_LABELS, 'Approve') + '</button>' +
          '<button type="button" class="mod-btn danger" data-mentor="' + m.id + '" data-decision="rejected">' + label(ACTION_LABELS, 'Reject') + '</button>' +
        '</div>';
      list.appendChild(row);
    });

    list.querySelectorAll('.mod-btn').forEach((b) => {
      b.addEventListener('click', () => {
        const id = Number(b.getAttribute('data-mentor'));
        const decision = b.getAttribute('data-decision');
        const decisions = loadJson(MENTOR_DECISIONS_KEY);
        decisions.push({ id: id, decision: decision, ts: Date.now() });
        localStorage.setItem(MENTOR_DECISIONS_KEY, JSON.stringify(decisions));
        if (decision === 'approved' && window.IH_MENTORS) {
          const applicant = SAMPLE_MENTORS.find((m) => m.id === id);
          const [trackLabel, experienceLabel] = applicant.meta.split(' · ');
          const track = trackLabel === 'Radiology' ? 'radiology' : 'preauth';
          window.IH_MENTORS.upsertMentor({
            id: 'admin-' + id,
            name: applicant.name.replace('Applicant', 'Mentor'),
            track: track,
            experience: experienceLabel,
            bio: 'Approved via the Owner Console mentor-review queue.',
            source: 'admin-sample',
          });
        }
        renderMentorQueue();
      });
    });
  }

  function renderMeetingsOverview() {
    const list = document.getElementById('meetingsOverviewList');
    if (!list || !window.IH_MENTORS) return;
    const meetings = window.IH_MENTORS.getMeetingRequests().slice().sort((a, b) => b.createdAt - a.createdAt);

    document.getElementById('meetingsPendingCount').textContent = meetings.filter((m) => m.status === 'pending').length;
    document.getElementById('meetingsAcceptedCount').textContent = meetings.filter((m) => m.status === 'accepted').length;
    document.getElementById('meetingsDeclinedCount').textContent = meetings.filter((m) => m.status === 'declined').length;

    list.innerHTML = '';
    if (!meetings.length) {
      list.innerHTML = '<p class="empty-note">' + t('noMeetings') + '</p>';
      return;
    }
    meetings.forEach((m) => {
      const row = document.createElement('div');
      row.className = 'mentor-row';
      const statusLabel = label(MEETING_STATUS_LABELS, m.status);
      row.innerHTML =
        '<div><div class="mentor-name">' + m.studentName + ' <i class="fi fi-rr-arrow-small-right"></i> ' + m.mentorName + '</div>' +
        '<div class="mentor-meta">' + (m.preferredDate || t('noDateSet')) + (m.note ? ' · ' + m.note : '') + '</div></div>' +
        '<span class="meeting-status status-' + m.status + '">' + statusLabel + '</span>';
      list.appendChild(row);
    });
  }

  function escapeHtml(str) {
    const div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
  }

  function courseFeedbackLabel(courseId) {
    const dictKey = COURSE_FEEDBACK_LABELS[courseId];
    return dictKey ? dictLabel(dictKey) : courseId;
  }

  function renderCourseFeedback() {
    const list = document.getElementById('courseFeedbackList');
    if (!list) return;
    const entries = loadJson(COURSE_FEEDBACK_KEY).slice().sort((a, b) => b.ts - a.ts);
    list.innerHTML = '';
    if (!entries.length) {
      list.innerHTML = '<p class="empty-note">' + t('noFeedback') + '</p>';
      return;
    }
    entries.forEach((f) => {
      const row = document.createElement('div');
      row.className = 'mod-row';
      const stars = '★'.repeat(f.stars || 0) + '☆'.repeat(5 - (f.stars || 0));
      const studentName = f.name ? escapeHtml(f.name) : t('anonymousStudent');
      const dateStr = f.ts ? new Date(f.ts).toLocaleDateString() : '';
      row.innerHTML =
        '<div><div class="mod-title">' + stars + ' — ' + studentName + '</div>' +
          (f.comment ? '<div class="mod-meta">' + escapeHtml(f.comment) + '</div>' : '') +
          '<div class="mod-meta">' + courseFeedbackLabel(f.course) + (dateStr ? ' · ' + dateStr : '') + '</div>' +
        '</div>' +
        '<div class="mod-actions"><button type="button" class="mod-btn danger" data-feedback="' + f.id + '">' + t('removeFeedback') + '</button></div>';
      list.appendChild(row);
    });

    list.querySelectorAll('[data-feedback]').forEach((b) => {
      b.addEventListener('click', () => {
        if (!confirm(t('confirmRemoveFeedback'))) return;
        const id = b.getAttribute('data-feedback');
        const remaining = loadJson(COURSE_FEEDBACK_KEY).filter((f) => f.id !== id);
        localStorage.setItem(COURSE_FEEDBACK_KEY, JSON.stringify(remaining));
        renderCourseFeedback();
      });
    });
  }

  // BACKUP & RESTORE — bundles every "ih-" localStorage key (student progress,
  // mentor directory, meeting requests, admin decisions) into one JSON file.
  function exportAllData() {
    const data = {};
    for (let i = 0; i < localStorage.length; i++) {
      const key = localStorage.key(i);
      if (key && key.indexOf('ih-') === 0) data[key] = localStorage.getItem(key);
    }
    const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'ihealth-academy-backup-' + new Date().toISOString().slice(0, 10) + '.json';
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
  }

  function importAllData(file) {
    const note = document.getElementById('backupStatusNote');
    const reader = new FileReader();
    reader.onload = () => {
      try {
        const data = JSON.parse(reader.result);
        Object.keys(data).forEach((key) => {
          if (key.indexOf('ih-') === 0) localStorage.setItem(key, data[key]);
        });
        note.textContent = t('backupRestored');
        location.reload();
      } catch (e) {
        note.textContent = t('backupBadFile');
      }
    };
    reader.readAsText(file);
  }

  function initBackup() {
    const exportBtn = document.getElementById('exportDataBtn');
    const importBtn = document.getElementById('importDataBtn');
    const importFile = document.getElementById('importDataFile');
    if (exportBtn) exportBtn.addEventListener('click', exportAllData);
    if (importBtn && importFile) {
      importBtn.addEventListener('click', () => importFile.click());
      importFile.addEventListener('change', () => {
        if (importFile.files[0]) importAllData(importFile.files[0]);
      });
    }
  }

  // REVENUE — reads only through window.IH_REVENUE (js/revenue.js), never localStorage
  // directly, so swapping that module for a live PayPal/Stripe API needs no changes here.
  function formatMoney(amount, currency) {
    const symbol = currency === 'USD' || !currency ? '$' : currency + ' ';
    return symbol + amount.toFixed(2);
  }

  function renderRevenue() {
    if (!window.IH_REVENUE) return;
    const summary = window.IH_REVENUE.getSummary();

    document.getElementById('kpiPaidMembers').textContent = summary.hasData ? summary.paidMembers : '—';
    document.getElementById('kpiMRR').textContent = summary.hasData ? formatMoney(summary.mrr, summary.currency) : '—';
    document.getElementById('revenueFree').textContent = summary.hasData ? formatMoney(summary.revenueByPlan.Free, summary.currency) : '—';
    document.getElementById('revenuePro').textContent = summary.hasData ? formatMoney(summary.revenueByPlan.Pro, summary.currency) : '—';
    document.getElementById('revenueInstitution').textContent = summary.hasData ? formatMoney(summary.revenueByPlan.Institution, summary.currency) : '—';
    document.getElementById('revenueNote').textContent = summary.hasData
      ? t('revenueConnectedNote').replace('{count}', window.IH_REVENUE.getTransactions().length)
      : t('revenueNotConnected');

    const list = document.getElementById('revenueTransactionsList');
    const transactions = window.IH_REVENUE.getTransactions().slice().reverse();
    list.innerHTML = '';
    if (!transactions.length) {
      list.innerHTML = '<p class="empty-note">' + t('noTransactions') + '</p>';
      return;
    }
    transactions.slice(0, 20).forEach((tx) => {
      const row = document.createElement('div');
      row.className = 'mentor-row';
      row.innerHTML =
        '<div><div class="mentor-name">' + (tx.name || tx.email || t('unknownPayer')) + '</div>' +
        '<div class="mentor-meta">' + tx.date + ' · ' + label(PLAN_LABELS, tx.plan) + '</div></div>' +
        '<div style="font-weight:700;">' + formatMoney(tx.amount, tx.currency) + '</div>';
      list.appendChild(row);
    });
  }

  function initRevenueImport() {
    const btn = document.getElementById('importRevenueBtn');
    const file = document.getElementById('importRevenueFile');
    if (!btn || !file || !window.IH_REVENUE) return;
    btn.addEventListener('click', () => file.click());
    file.addEventListener('change', () => {
      const f = file.files[0];
      if (!f) return;
      const reader = new FileReader();
      reader.onload = () => {
        const result = window.IH_REVENUE.importCSV(reader.result);
        renderRevenue();
        alert(t('transactionsImportedAlert').replace('{count}', result.imported));
        file.value = '';
      };
      reader.readAsText(f);
    });
  }

  initLock();
  renderModQueue();
  renderMentorQueue();
  renderMeetingsOverview();
  renderCourseFeedback();
  renderRevenue();
  initRevenueImport();
  initBackup();

  // Re-render every dynamically-built queue/table so their labels/statuses
  // switch language too — the shared i18n.apply() only re-tags static [data-i18n]
  // elements, which doesn't reach the innerHTML this file rebuilds from data.
  document.addEventListener('ih:langchange', () => {
    const lockError = document.getElementById('adminLockError');
    if (lockError && lockError.textContent) lockError.textContent = t('incorrectPasscode');
    renderModQueue();
    renderMentorQueue();
    renderMeetingsOverview();
    renderCourseFeedback();
    renderRevenue();
  });
})();
